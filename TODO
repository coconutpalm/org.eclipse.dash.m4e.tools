TODO

- Find lonely sources and mention them in the report
- A command to deploy (part of) a repo to a server (incl. redeploy of releases -> delete those artifacts in the local m2 repo)
	- Add button/menu in m2view
- A command to refresh (part of) the local m2 repo (so Maven will download the artifacts again)
	- Add button/menu in m2view
- Patch: Add links to project home page from POM
